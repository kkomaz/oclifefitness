$column-check: <%= Rails.env.production? ? 'hide' : 'show-columns' %>;

$susy: (
  columns: 12,
  column-width: 90px
);

$susy-phone: (
  columns: 4
);

$susy-debug: (
  debug: (
    image: hide,
    color: rgba(153,255,153, 0.2),
    output: background
  )
);
